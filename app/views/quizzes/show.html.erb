<h2><%= @quiz.name %></h2>
<%= link_to "Edit", edit_quiz_path(@quiz)  %>
<ul>
	<% @questions.each do |question| %>
		<li>
			<%= question.title %>
			<ul>
				<% question.answers.each do |answer| %>
					<li><%= answer.body %></li>
				<% end %>	
			</ul>
		</li>
	<% end %>
</ul>